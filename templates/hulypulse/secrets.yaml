apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: hulypulse
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: 'true'
spec:
  encryptedData:
    redisSecret: {{.Values.hulypulse.secrets.redisSecret}}

